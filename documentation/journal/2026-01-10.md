
Because the Hydra built tarball requires additional configuration even after Terraform provisions it, I might as well build the tarball myself using nixos-generator.

``nixos-generate -f proxmox-lxc``